#include "main.h"

/**
  * print_sign - shuf desc
  * Description: sin checkerr
  * @n: sign to check
  * Return: return 1 or 0 or -1
  */
int print_sign(int n)
{
	if (n > 0)
	{
		return (1);
	}

	if (n == 0)
	{
		return (0);
	}
	if (n < 0)
	{
		return (-1);
	}
}
