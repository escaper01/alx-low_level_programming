#include "main.h"

/**
  * _abs- shuf desc
  * Description: return absr
  * @n: sign to check
  * Return: return abs val
  */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (-1 * n);
	}
}
