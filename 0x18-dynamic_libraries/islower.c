#include "main.h"
/**
  * _islower - shuf desc
  * Description: lowercase checker
  * @c: letter to check
  * Return: 1 or 0
  */
int _islower(int c)
{
	if (c >= 97)
	{
		return (1);
	}
	return (0);
}
